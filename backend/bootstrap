#!/bin/sh

# Lambda Bootstrap for Java 17
# The fat JAR has been extracted, so all classes are available at the root.
# Delegate to Lambda's standard Java 17 runtime bootstrap to find and load the handler.

set -euo pipefail

# Lambda's java17 runtime will automatically add the task root directory to classpath
# No explicit classpath configuration needed
exec /var/runtime/bootstrap
